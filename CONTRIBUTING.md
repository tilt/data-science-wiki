# Contributing

This wiki is intended to become public.

Do not commit API keys, tokens, personal contact details, private notes, employer-confidential information, client data, private CV metadata, or local filesystem paths.

Before committing:

```sh
make validate
make build
git diff --check
```
