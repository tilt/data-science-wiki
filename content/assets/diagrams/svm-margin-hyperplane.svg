<svg xmlns="http://www.w3.org/2000/svg" width="720" height="480" viewBox="0 0 720 480" role="img" aria-labelledby="title desc">
  <title id="title">A support vector machine maximizes the margin around the decision boundary</title>
  <desc id="desc">Two classes of points separated by a vertical decision boundary. Dashed margin lines pass through the closest points of each class, the support vectors, and the gap between them is the margin of width two over the norm of w.</desc>
  <style>
    text { fill: #111827; font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; }
    .muted { fill: #374151; }
    .small { font-size: 15px; }
    .axis { stroke: #cbd5e1; stroke-width: 1.5; }
    .boundary { stroke: #111827; stroke-width: 3; }
    .margin { stroke: #6b7280; stroke-width: 2; stroke-dasharray: 6 5; }
    .pos { fill: #16a34a; }
    .neg { fill: #dc2626; }
    .sv { fill: none; stroke: #2563eb; stroke-width: 2.5; }
    .warrow { stroke: #7c3aed; stroke-width: 2.5; }
    .mspan { stroke: #c2410c; stroke-width: 2; }
  </style>
  <defs>
    <marker id="wtip" markerWidth="10" markerHeight="10" refX="7" refY="3" orient="auto">
      <path d="M0,0 L7,3 L0,6 Z" fill="#7c3aed"/>
    </marker>
  </defs>

  <text x="360" y="36" text-anchor="middle" font-size="24" font-weight="800">Maximum-margin classifier</text>
  <text x="360" y="64" text-anchor="middle" font-size="15" class="muted">The boundary is placed as far as possible from the nearest points of each class.</text>

  <!-- faint axes -->
  <line class="axis" x1="90" y1="370" x2="630" y2="370"/>
  <line class="axis" x1="360" y1="86" x2="360" y2="382"/>

  <!-- margin lines at x = -2 and x = +2 -->
  <line class="margin" x1="230" y1="86" x2="230" y2="370"/>
  <line class="margin" x1="490" y1="86" x2="490" y2="370"/>
  <!-- decision boundary x = 0 -->
  <line class="boundary" x1="360" y1="86" x2="360" y2="94"/>
  <line class="boundary" x1="360" y1="132" x2="360" y2="370"/>
  <text x="360" y="408" text-anchor="middle" class="small">boundary: w·x + b = 0</text>

  <!-- margin width span -->
  <line class="mspan" x1="230" y1="122" x2="490" y2="122"/>
  <line class="mspan" x1="230" y1="114" x2="230" y2="130"/>
  <line class="mspan" x1="490" y1="114" x2="490" y2="130"/>
  <text x="360" y="109" text-anchor="middle" class="small">margin = 2 / ‖w‖</text>

  <!-- w direction arrow -->
  <line class="warrow" x1="360" y1="255" x2="438" y2="255" marker-end="url(#wtip)"/>
  <text x="399" y="244" text-anchor="middle" class="small">w</text>

  <!-- positive class points (right) with support vectors circled -->
  <circle class="pos" cx="490" cy="302" r="7"/><circle class="sv" cx="490" cy="302" r="12"/>
  <circle class="pos" cx="490" cy="165" r="7"/><circle class="sv" cx="490" cy="165" r="12"/>
  <circle class="pos" cx="575" cy="230" r="7"/>
  <text x="575" y="345" text-anchor="middle" class="small pos" fill="#15803d">class +1</text>

  <!-- negative class points (left) with support vectors circled -->
  <circle class="neg" cx="230" cy="302" r="7"/><circle class="sv" cx="230" cy="302" r="12"/>
  <circle class="neg" cx="230" cy="165" r="7"/><circle class="sv" cx="230" cy="165" r="12"/>
  <circle class="neg" cx="145" cy="230" r="7"/>
  <text x="145" y="345" text-anchor="middle" class="small neg" fill="#b91c1c">class −1</text>

  <!-- support vector legend -->
  <circle class="sv" cx="242" cy="440" r="9"/>
  <text x="260" y="445" class="small">support vectors: closest points that touch the margin</text>
</svg>
